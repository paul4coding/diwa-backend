target/
*.class
*.jar
*.war
*.ear
*.log
uploads/

# ── Secrets — NE JAMAIS COMMITTER ────────────────────────────────
.env
application.properties
application-dev.properties
application-local.properties
# application-prod.properties est COMMITÉ car il ne contient que des ${ENV_VAR} (pas de secrets)
